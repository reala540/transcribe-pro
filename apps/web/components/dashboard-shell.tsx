import Link from "next/link";
import { File as FileAudio, KeyRound, LayoutDashboard, LogOut, Settings } from "lucide-react";
const nav = [{ href: "/dashboard", label: "Overview", icon: LayoutDashboard },{ href: "/dashboard/projects", label: "Projects", icon: FileAudio },{ href: "/dashboard/api", label: "API Keys", icon: KeyRound },{ href: "/dashboard/settings", label: "Settings", icon: Settings }];
export function DashboardShell({ children }: { children: React.ReactNode }) { return <div className="min-h-screen bg-zinc-950 text-zinc-100"><div className="mx-auto grid min-h-screen max-w-7xl grid-cols-1 md:grid-cols-[240px_1fr]"><aside className="border-r border-zinc-800 p-6"><Link href="/" className="text-lg font-semibold tracking-tight">TranscribePro</Link><nav className="mt-8 space-y-2">{nav.map((item) => { const Icon = item.icon; return <Link key={item.href} href={item.href} className="flex items-center gap-3 rounded-xl px-3 py-2 text-sm text-zinc-300 transition hover:bg-zinc-900 hover:text-white"><Icon className="h-4 w-4" />{item.label}</Link>; })}</nav><form action="/auth/signout" method="post" className="mt-8"><button className="flex items-center gap-3 rounded-xl px-3 py-2 text-sm text-zinc-300 transition hover:bg-zinc-900 hover:text-white"><LogOut className="h-4 w-4" />Sign out</button></form></aside><main className="p-6 md:p-10">{children}</main></div></div>; }
